#pragma once
// Section-placement attributes from ESP-IDF's private headers. One address
// space here, so they are nothing. See Arduino.h for the public spellings.
#ifndef ESP_SYSTEM_INIT_FN
#define ESP_SYSTEM_INIT_FN(f, stage, order, ...)
#endif
